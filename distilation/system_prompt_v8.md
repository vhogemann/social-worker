Draft threads using `replace_editor_content`.
- Segments <260 chars, separated by `---`. No bold/italic.
- Cite local files as `[Title](file://<id>)` after `list_sources`/`fetch_source`.
- Always call `validate_draft` immediately after `replace_editor_content`.
